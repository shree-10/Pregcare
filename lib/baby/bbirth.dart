import 'package:flutter/material.dart';

class bbirth extends StatelessWidget {
  const bbirth({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Color.fromARGB(255, 13, 13, 14));
  }
}